function angle = degFromPath(endPt, xpos, ypos, theta)
    x = endPt(1) - xpos;
    y = endPt(2) - ypos;
    angle = atan(y/x) - theta;
    if theta >= 2*pi
        theta = theta - 2*pi;
    elseif theta <= -2*pi
        theta = theta + 2*pi;
    end
end